export default function AppLogoIcon({ src = 'assets/images/logo-polines.png', alt = 'Logo', width = 50, height = 50, className = '' }) {
    return <img src={src} alt={alt} width={width} height={height} className={className} />;
}
